'use client';

import { Toaster } from 'react-hot-toast';

const ToasterContext = () => {
    return <Toaster />;
};

export default ToasterContext;
 
 //Now, we can use the  ToasterContext  component in our  App.tsx  file.